// Change this to your PC's local IP address if testing on a real device (e.g., '192.168.1.5')
// Use '10.0.2.2' ONLY if you are testing on an Android Emulator
export const SERVER_IP = '10.14.107.99'; 
export const API_URL = `http://${SERVER_IP}:3001`;
